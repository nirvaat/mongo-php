# Screenshots

Place screenshots referenced from the project README here. Expected filenames:

- `server.png` — the Server view (home), showing the database list and create-DB form.
- `browse.png` — a populated Browse tab on some collection.
- `query.png` — the Query tab with an aggregation pipeline and its result.

Suggested width: **1400px**. PNG is fine; WebP also OK.

If you change the filenames or add more, update the image table in `../../README.md` to match.
