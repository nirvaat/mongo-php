## What this PR does

<!-- One or two sentences. -->

## Why

<!-- The problem this solves, or the feature it adds. Link an issue if there is one. -->

Closes #

## How to test

1.
2.
3.

## Screenshots

<!-- Required for UI changes. Before / after if relevant. -->

## Checklist

- [ ] `php -l` passes for every changed file
- [ ] Output of new code is escaped with `h()` where it hits the page
- [ ] State-changing actions go through `action.php` and are CSRF-protected
- [ ] Destructive actions require typed-name (or `DELETE`) confirmation
- [ ] CHANGELOG.md updated under `[Unreleased]`
- [ ] README.md / USAGE.md updated if user-visible behaviour changed
